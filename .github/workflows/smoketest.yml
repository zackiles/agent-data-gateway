name: Smoketest
on:
  pull_request:
    branches: [main]

jobs:
  smoketest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x
      - name: Run smoketest
        run: ./scripts/smoketest.sh
